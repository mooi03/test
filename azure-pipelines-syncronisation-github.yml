trigger:
  branches:
    include:
      - main

pool:
  name: 'default'

steps:
- checkout: self
  persistCredentials: true
  fetchDepth: 0

- script: |
    git clone "https://tlonchampt2@dev.azure.com/tlonchampt2/armand/_git/armand"
    cd
    git checkout
    git log
    git branch
    echo "pull"
    git pull origin main
  displayName: "Synchroniser GitHub main avec Azure DevOps main"

