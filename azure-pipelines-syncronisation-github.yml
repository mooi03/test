trigger:
  branches:
    include:
      - main

pool:
  name: 'default'

steps:
- checkout: self
  persistCredentials: true
  fetchDepth: 0

- script: |
    echo "branch"
    git branch
    echo "checkout"
    git checkout
  displayName: "Synchroniser GitHub main avec Azure DevOps main"
